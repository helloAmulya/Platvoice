
// This file will contain the Zod schemas for validating business-related data.

/*
import { z } from 'zod';

export const businessCreationSchema = z.object({
  name: z.string().min(2, 'Business name must be at least 2 characters long'),
  address: z.string().optional(),
});
*/
